cadena = "/******************************************************************* ============================================================== ** ============================================================== ** ============================================================== * * ===================ARCHIVO DE PRUEBA DE JS==================== * * ============================================================== * * ================PATHL -> /home/user/output/js/================ * * ============================================================== * * ============================================================== * * ============================================================== * ******************************************************************/"

encontrar = cadena.find("PATHL ->") + len("PATHL ->")
parte1 = cadena[encontrar:].replace("user", "rau")
encontrar2 = parte1.find("=")
print(parte1[:encontrar2])

pila = []
pila.append("START")
print(len(pila))
print(pila[len(pila) -1])
while len(pila) != 0:

    if pila[len(pila) -1] == "START":
        print("ultimo es pila")
        pila.pop()
        pila.append("(")
        pila.append("V")
        pila.append("OP")
        pila.append(")")
        break
        pass
    pass

print(pila)